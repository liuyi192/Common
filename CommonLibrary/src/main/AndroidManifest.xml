<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xiaozhu.library">

    <application
        android:allowBackup="true"
        android:supportsRtl="true">
        <!--查看图集-->
        <activity
            android:name=".activity.CheckAtlasActivity"
            android:theme="@style/CheckAtlasTheme" />
        <!--查看相册-->
        <activity android:name=".activity.PhotoAlbumActivity" />
        <!--异常界面-->
        <activity
            android:name=".activity.ExceptionActivity"
            android:theme="@style/customProgressDialog" />
        <!--下载界面-->
        <activity
            android:name=".download.DownloadFileActivity"
            android:theme="@style/customProgressDialog" />
        <service android:name=".download.DownloadService" />
    </application>
</manifest>
